# GitHub 上传说明

将本文件夹 **内部的所有内容** 上传到 GitHub 仓库 `xait-mma` 的根目录。

不要把 `GitHub上传文件` 这个文件夹本身作为额外一层上传。

上传完成后，仓库第一层应直接显示：

```text
index.html
training.html
styles.css
script.js
assets/
resources/
```

## 应上传

- 所有 `.html`、`.css`、`.js` 文件
- `assets/` 文件夹及协会图片
- `resources/templates/模板.docx`
- `README.md`

## 不在这里上传

全国大学生数学建模竞赛真题和后续优秀论文不放在此文件夹。

它们保存在上一级目录的：

```text
本地资料备份/
```

大文件通过 GitHub 的“发布版本”上传，网站会使用发布版本下载链接。

## 发布网站

上传完成后，在 GitHub 仓库中依次打开：

```text
设置 → Pages → 生成和部署 → 从分支部署
```

选择：

```text
分支：main
文件夹：/(根目录)
```
